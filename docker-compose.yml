version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    command: uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000} --reload
    volumes:
      - ./api:/app/api
      - ./model:/app/model
      - ./metrics:/app/metrics
